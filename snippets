// Convert PX to REM. 
// 

$rootFont = 16px;

@function calculateRem($size) {
    $remSize: $size / $rootFont;
    @return #{$remSize}rem;
}

@mixin fontSize($size) {
    font-size: $size; //Fallback in px
    font-size: calculateRem($size);
}
